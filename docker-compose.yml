version: '3'

services:
  frontpages:
    build: .
    ports:
      - "15001:15001"
    restart: unless-stopped
    volumes:
      - ./config.yaml:/app/config.yaml:ro
    deploy:
      resources:
        limits:
          memory: 256M
        reservations:
          memory: 256M